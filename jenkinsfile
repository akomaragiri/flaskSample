pipeline {
  
  agent any
  
  stages {
	
	stage('Deploy') {
      steps {
		sh "printenv | grep PATH"
		bat "eb deploy"
		bat "aws s3 cp webapp.zip s3://${env.S3_BUCKET}/FlaskApp/webapp.zip"
      }
    }
  }
}
